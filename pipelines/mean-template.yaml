description: |
  Mean template
imports:
  - ${PROC_DECAM_DIR}/pipelines/template.yaml

tasks:
  assembleCoadd:
    class: lsst.drp.tasks.assemble_coadd.CompareWarpAssembleCoaddTask
    config:
      statistic: MEAN
      assembleStaticSkyModel.statistic: MEAN
