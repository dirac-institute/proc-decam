computeSite: local

project: "proc-decam"
campaign: "proc-decam"
operator: "${USER}"

payload:
  butlerConfig: ${REPO}/butler.yaml
      
parsl:
  log_level: DEBUG

  # submitPath: "${PROC_LSST_SUBMIT_PATH}"
wmsServiceClass: lsst.ctrl.bps.parsl.ParslService
subDirTemplate: '{label}/{tract}/{patch}/{band}/{subfilter}/{physical_filter}/{visit}/{exposure}'

site:
  local:
    class: proc_decam.parsl.sites.Local
    cores: 64
    monitorEnable: true # enable the MonitoringHub
    strategy: simple
